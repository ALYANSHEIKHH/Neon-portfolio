// components/app.tsx
import "swiper/css";
import "swiper/css/effect-coverflow";
import "swiper/css/pagination";

const AppStyles = () => null; // This component doesn't render anything, just ensures global imports

export default AppStyles;
